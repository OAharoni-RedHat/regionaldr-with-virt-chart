---
# One-time disable of Argo CD automated sync on the regional-dr Application after
# PostSync settlement (independent of DRPC health / managed workloads).
- name: Disable Argo CD automated sync
  hosts: localhost
  gather_facts: false
  roles:
    - argocd_sync_disable
