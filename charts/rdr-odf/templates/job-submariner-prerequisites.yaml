apiVersion: batch/v1
kind: Job
metadata:
  name: submariner-prerequisites-checker
  namespace: open-cluster-management
  labels:
    app.kubernetes.io/name: submariner-prerequisites
    app.kubernetes.io/component: health-check
  annotations:
    argocd.argoproj.io/sync-wave: "7"
    argocd.argoproj.io/sync-options: Prune=false
    argocd.argoproj.io/compare-options: IgnoreExtraneous
spec:
  backoffLimit: 50
  activeDeadlineSeconds: 14400
  ttlSecondsAfterFinished: 300
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: prerequisites-checker
        image: {{ .Values.ansible.containerImage }}
        env:
        - name: PRIMARY_CLUSTER
          value: {{ .Values.global.primaryClusterName | quote }}
        - name: SECONDARY_CLUSTER
          value: {{ .Values.global.secondaryClusterName | quote }}
        - name: SUBMARINER_BROKER_NAMESPACE
          value: {{ printf "%s-broker" (.Values.global.clusterSetName | default "resilient") | quote }}
        command:
        - /bin/bash
        - -c
        - |
            STAGE=$(mktemp -d)
            for f in /ansible-cm/*; do
              key=$(basename "$f")
              rel="${key//__//}"
              mkdir -p "$STAGE/$(dirname "$rel")"
              cp "$f" "$STAGE/$rel"
            done
            export PRIMARY_CLUSTER SECONDARY_CLUSTER SUBMARINER_BROKER_NAMESPACE STAGE
            SUB_SCRIPT="$STAGE/roles/submariner_prerequisites/files/submariner-prerequisites-check.sh"
            echo "Running Submariner prerequisites script (STAGE=$STAGE)."
            exec bash "$SUB_SCRIPT"
        volumeMounts:
        - name: ansible
          mountPath: /ansible-cm
          readOnly: true
      volumes:
      - name: ansible
        configMap:
          name: regionaldr-ansible
      serviceAccountName: submariner-prerequisites-checker
